# ADR-0000: (결정을 한 줄로. 명사구가 아니라 서술문으로 — "X를 Y로 한다")

- **Status**: Proposed | Accepted | Deprecated | Superseded by [ADR-XXXX](XXXX-slug.md)
- **Date**: YYYY-MM-DD (결정한 날. 문서 쓴 날이 아님)
- **관련**: 커밋/PR/계획 문서 링크

## Context

어떤 상황이었고 무엇이 결정을 강제했는가. 여기서는 아직 결론을 쓰지 않습니다.

- 문제: 무엇이 잘못되고 있었나 (가능하면 관측된 증상으로)
- 제약: 바꿀 수 없었던 것 (스키마, 외부 계약, 기존 데이터, 라이브러리 한계)
- 검토한 대안과 버린 이유 — **이 부분이 ADR의 값어치입니다**

## Decision

무엇을 하기로 했는가. 능동태 현재형으로 씁니다 — "~한다".

구현 위치를 함께 적어 6개월 뒤에도 코드에서 찾을 수 있게 합니다:
`src/path/file.ts:함수명`, `src-tauri/src/path.rs`

## Consequences

결정이 만들어낸 결과. 좋은 것과 나쁜 것을 모두 씁니다.

- **얻은 것**:
- **잃은 것 / 감수하는 것**:
- **따라오는 의무**: 이 결정 때문에 앞으로 계속 지켜야 하는 것 (마이그레이션, 배포 절차, 재실행 안전성 등)
